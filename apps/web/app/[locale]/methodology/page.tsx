import { isLocale } from "@/lib/i18n"; import { notFound } from "next/navigation";
export default async function Methodology({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <main className="container"><header className="page-head"><h1>{locale==="zh"?"方法论":"Methodology"}</h1><p className="lead">{locale==="zh"?"先预测联合比分分布，再聚合为胜平负和晋级概率。":"We forecast a joint score distribution first, then derive match and advancement probabilities."}</p></header><div className="grid two">{[["1","Elo / dynamic strength"],["2","Dixon-Coles score model"],["3","Market consensus"],["4","Out-of-time ensemble"],["5","Multiclass calibration"],["6","Tournament Monte Carlo"]].map(([n,title])=><section className="card" key={n}><div className="eyebrow">STEP {n}</div><h2>{title}</h2><p className="muted">{locale==="zh"?"每一层均保存输入截止时间、版本与来源，用滚动样本外预测决定是否进入最终模型。":"Each layer stores cutoff, version and provenance, and enters the ensemble only after rolling out-of-time validation."}</p></section>)}</div></main>}
