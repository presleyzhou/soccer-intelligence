import { isLocale } from "@/lib/i18n"; import { notFound } from "next/navigation";
export default async function Disclaimer({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <main className="container"><header className="page-head"><h1>{locale==="zh"?"免责声明":"Disclaimer"}</h1></header><section className="card"><p>{locale==="zh"?"实时比分与市场数据可能因供应商延迟、修正或中断而变化。预测仅在明确标注为模型输出时出现，不保证准确，也不构成博彩或投资建议。用户须遵守所在地区法律和年龄限制。本网站不是 FIFA 官方网站。":"Live scores and market data may change because of provider delays, corrections, or outages. Forecasts appear only when explicitly labelled as model outputs, may be wrong, and are not betting or investment advice. Users must follow local laws and age restrictions. This site is not affiliated with FIFA."}</p></section></main>}
