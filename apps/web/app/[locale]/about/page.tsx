import { isLocale } from "@/lib/i18n"; import { notFound } from "next/navigation";
export default async function About({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <main className="container"><header className="page-head"><h1>{locale==="zh"?"关于项目":"About"}</h1><p className="lead">{locale==="zh"?"Soccer Intelligence 是独立的国际足球实时数据与可解释预测平台。事实数据与模型预测严格分离。":"Soccer Intelligence is an independent platform for live international football data and explainable forecasts. Facts and model predictions are kept strictly separate."}</p></header></main>}
