import { sources } from "@/lib/data"; import { isLocale, formatDate } from "@/lib/i18n"; import { notFound } from "next/navigation";
export default async function Sources({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <main className="container"><header className="page-head"><h1>{locale==="zh"?"数据来源":"Data sources"}</h1></header><div className="grid two">{sources.map(s=><section className="card" key={s.id}><div className="eyebrow">{s.category}</div><h2>{s.name}</h2><p className="muted">Status: {s.status} · {formatDate(s.updatedAt,locale)}</p>{s.url?<a className="button" href={s.url}>Provider documentation</a>:null}</section>)}</div></main>}
